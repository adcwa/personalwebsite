{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "public" } },
    { "src": "config.toml", "use": "@vercel/hugo" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
} 